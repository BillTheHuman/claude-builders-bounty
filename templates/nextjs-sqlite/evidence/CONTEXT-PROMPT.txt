Read the project instructions already loaded into your context. Without tools or follow-up questions, return only JSON with framework (including version/router), database, package_manager, migration_command, check_command, server_components_default (boolean), questions (array). State the defaults specified in CLAUDE.md, not your preferences.
