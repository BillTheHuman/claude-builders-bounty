[
  {
    "case": "missing-optional-key",
    "path": "example.py",
    "requirement": "Missing optional name is allowed and must return None.",
    "before": "def name_of(profile):\n    return profile.get(\"name\")\n",
    "after": "def name_of(profile):\n    return profile[\"name\"]\n",
    "expected_defect": true,
    "required_evidence_terms_any": [
      "keyerror",
      "missing",
      "key"
    ]
  },
  {
    "case": "zero-timeout",
    "path": "example.js",
    "requirement": "A timeout of zero disables timing. Only null or undefined use the default.",
    "before": "export function timeout(o) {\n  return o.timeout ?? 500;\n}\n",
    "after": "export function timeout(o) {\n  return o.timeout || 500;\n}\n",
    "expected_defect": true,
    "required_evidence_terms_any": [
      "zero",
      "0",
      "falsy"
    ]
  },
  {
    "case": "intentional-lowercase",
    "path": "example.py",
    "requirement": "Change normalize to lowercase; backwards compatibility with uppercase is expressly not required.",
    "before": "def normalize(value):\n    return value.upper()\n",
    "after": "def normalize(value):\n    return value.lower()\n",
    "expected_defect": false,
    "required_evidence_terms_any": []
  },
  {
    "case": "slice-last-item",
    "path": "example.py",
    "requirement": "Retain all input elements, returning an independent list.",
    "before": "def copy_items(values):\n    return values[:]\n",
    "after": "def copy_items(values):\n    return values[:-1]\n",
    "expected_defect": true,
    "required_evidence_terms_any": [
      "last",
      "drop",
      "omit"
    ]
  },
  {
    "case": "guard-preserved",
    "path": "example.py",
    "requirement": "Refactor while preserving the empty-list result and normal average.",
    "before": "def average(values):\n    if len(values) == 0:\n        return 0\n    return sum(values) / len(values)\n",
    "after": "def average(values):\n    if not values:\n        return 0\n    return sum(values) / len(values)\n",
    "expected_defect": false,
    "required_evidence_terms_any": []
  },
  {
    "case": "tuple-member-not-return",
    "path": "example.py",
    "requirement": "An omitted description is represented by an empty string in the SECOND tuple field.",
    "before": "def record(label, description):\n    return label, description\n",
    "after": "def record(label, description):\n    return label, description or \"\"\n",
    "expected_defect": false,
    "required_evidence_terms_any": []
  }
]