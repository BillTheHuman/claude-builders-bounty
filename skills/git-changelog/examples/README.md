# Actual public-repository sample

The adjacent CHANGELOG.md is the unedited output of the generator against the
recorded pallets/click HEAD. It was compared with Git's own commit-count result.
No Python code from the sample repository was executed.

Reproduce from a full clone using the HEAD and tag recorded in
REAL-REPO-RESULT.json. Checkout that HEAD before running the command; newer
commits or tags will naturally change the default range.
